import React from "react";
import dark from '../../media/ravenna/dark-ravenna.png'

const Closer = () => {

    return (
        <section className={`w-screen text-white h-[600px]
        bg-[url(${dark})]`}
  
        >
            <img src={dark}/>
            slat
        </section>
    )
}

export default Closer